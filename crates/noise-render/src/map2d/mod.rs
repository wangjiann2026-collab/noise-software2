//! 2D noise heatmap renderer (stub — full implementation in Phase 5).

pub struct Map2DRenderer;

impl Map2DRenderer {
    pub fn new() -> Self { Self }
}

impl Default for Map2DRenderer {
    fn default() -> Self { Self::new() }
}
