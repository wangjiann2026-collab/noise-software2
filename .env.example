# API server port (default 8080)
API_PORT=8080

# JWT signing secret — CHANGE THIS before production deployment
NOISE_JWT_SECRET=change-me-in-production-use-openssl-rand-base64-32

# SQLite database path inside container
NOISE_DB=/data/noise.db

# Log level: error, warn, info, debug, trace
RUST_LOG=info
